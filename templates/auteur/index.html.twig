{% extends 'base.html.twig' %}

{% block title %}Auteur index{% endblock %}

{% block body %}
    <div class="jumbotron">
        <h1 class="display-4">Auteur index</h1>
        <a class="btn btn-primary btn-lg" href="{{ path('admin_auteur_new') }}" role ="button">Create new</a>
    </div>

    <div class="container">
        <table class="table table-striped table-responsive">
            <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for auteur in auteurs %}
            <tr>
                <td>{{ auteur.id }}</td>
                <td>{{ auteur.nom }}</td>
                <td>{{ auteur.prenom }}</td>
                <td>
                    <a class="badge badge-light" href="{{ path('auteur_show', {'id': auteur.id}) }}">show</a>
                    <a class="badge badge-light" href="{{ path('admin_auteur_edit', {'id': auteur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
