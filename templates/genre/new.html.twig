{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}
    <div class="jumbotron">
        <a class="badge badge-light" href="{{ path('genre_index') }}">back to list</a>
        <h1>Create new Genre</h1>
    </div>

    {{ include('genre/_form.html.twig') }}

{% endblock %}
