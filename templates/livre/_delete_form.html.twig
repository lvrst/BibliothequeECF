<form method="post" action="{{ path('admin_livre_delete', {'id': livre.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer ce livre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livre.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
