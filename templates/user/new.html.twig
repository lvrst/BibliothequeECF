{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="jumbotron">
        <a class="badge badge-light" href="{{ path('user_index') }}">back to list</a>
        <h1>Create new User</h1>
    </div>

    {{ include('user/_form.html.twig') }}

{% endblock %}
